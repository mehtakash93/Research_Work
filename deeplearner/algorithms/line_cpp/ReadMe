1. Compile the code using the CompileLINECode.sh
2. Make sure that the edgelist.graph file is present in the folder which stores the networkx graph.
3. Run line_subprocess which will generate the vec_all.txt file containing the embedding.
