1. Run the getEdgeListandDictText to generate the edgelist from the BBC corpus. Output - corpusEdgeList.graph
2. Run the getEdgeListandDictKnowledgeGraph to generate the edgelist from the WN dataset. Output - knowledgeGraphEdgeList.graph
3. Run MergeGraphs to combine the two graphs and create edges between them if there is any common words.
